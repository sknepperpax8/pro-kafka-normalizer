# pro-kafka-normalizer
A Spring Cloud Streams application that processes messages in Kafka with the Kafka Streams API.

### development
Start a local kafka cluster before running the app.
```shell
$ docker-compose up -d
```
